services:
  mailpit:
    image: axllent/mailpit
    container_name: mailpit
    ports:
      - "1025:1025"   # SMTP
      - "8025:8025"   # Web UI
      - "1110:1110"   # POP3
    environment:
      MP_POP3_AUTH: "alice:password"
